{
  "name": "antigravity",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://idontknowcoding.com",
  "scripts": {
    "dev": "npx serve .",
    "build": "mkdir -p dist && cp index.html styles.css dist/ && cp -r public/* dist/",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}